---
title: "손실 — 틀림을 숫자 하나로 만든다"
description: "배우려면 점수가 필요합니다. 손실은 '얼마나 틀렸나'를 숫자 하나로 만듭니다 — 값 예측엔 제곱오차 (예측−정답)², 확률 예측엔 −log p(cross-entropy). 데이터를 고정하면 손실은 W의 함수, 즉 지형이 되고 학습은 낮은 곳 찾기가 됩니다. GPT의 사전학습 손실이 바로 다음 토큰 cross-entropy. 모델은 어떻게 배우나 시리즈 0편, 인터랙티브와 함께. 문장을 클릭하면 한/영 원문을 볼 수 있습니다."
pubDate: 2026-07-22T15:00:00
lang: ko
translationKey: loss-function
tags: ["AI", "딥러닝", "손실함수", "LLM"]
---

import BilingualArticle from "../../components/BilingualArticle.astro";
import SeriesNav from "../../components/SeriesNav.astro";
import SeriesNext from "../../components/SeriesNext.astro";
import { blocks } from "../../data/lossFunction";

<SeriesNav id="training" current="loss-function" lang="ko" />

<BilingualArticle blocks={blocks} />

<SeriesNext id="training" current="loss-function" lang="ko" />
