---
title: "글자가 벡터가 되기까지 — 토큰화와 임베딩"
description: "모델은 텍스트를 그대로 다루지 못합니다. 글자 → 토큰(서브워드·BPE) → ID → 벡터(임베딩) 순으로 바뀌고, 학습되는 건 임베딩 행렬 하나입니다. 비슷한 뜻이 가까이 모이고 king−man+woman≈queen이 되는 이유까지. 시리즈의 출발점(0편), 인터랙티브와 함께. 문장을 클릭하면 한/영 원문을 볼 수 있습니다."
pubDate: 2026-07-16T09:00:00
lang: ko
translationKey: tokens-embeddings
tags: ["AI", "임베딩", "토큰화", "딥러닝"]
---

import BilingualArticle from "../../components/BilingualArticle.astro";
import SeriesNav from "../../components/SeriesNav.astro";
import SeriesNext from "../../components/SeriesNext.astro";
import { blocks } from "../../data/tokensEmbeddings";

<SeriesNav id="transformer" current="tokens-embeddings" lang="ko" />

<BilingualArticle blocks={blocks} />

<SeriesNext id="transformer" current="tokens-embeddings" lang="ko" />
